{
  "scripts": {
    "build-css": "node-sass src/style.scss --quiet",
    "build": "npm run --silent build-css | node build.js > dist/resume.html",
    "start": "npm run --silent build-css | node build.js > public/resume.html && ecstatic public"
  },
  "dependencies": {
    "ecstatic": "^4.1.4",
    "hyperstream": "^1.2.2",
    "marked": "^1.2.0",
    "node-sass": "^4.14.1"
  }
}
